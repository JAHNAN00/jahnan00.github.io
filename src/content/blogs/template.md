---
title: 模板
date: 1970-01-01 00:00:00
tags:
 - 其它
categories:
 - 其它
---
文章内容

<!--
给后续 agent / 大模型的隐藏说明示例：
1. 这段使用 HTML 注释包裹，网页渲染时不可见。
2. 可在这里写本文的维护规则，例如目标读者、内容结构、是否保留作者口吻等。
3. 如果是系列文章，建议注明固定命名规则、categories 约束、tags 风格。
4. 不要在注释里写会误导读者的展示性正文，因为它不会在页面中出现。
-->

## 注意事项

* 文件名避免使用特殊字符，尤其是 `+`，以免影响站点路由与链接稳定性。
* 标题从两个#号开始。一个#号可能与页面级标题语义冲突。

## 推荐格式

* front matter 默认包含：`title`、`date`、`tags`、`categories`。
* `tags` 保持精简，优先保留 1 到 3 个核心标签。
* `categories` 尽量稳定，不要频繁发明新分类。
* 正文标题从两个 `#` 开始，避免一级标题影响文章页层级。
* 如果需要给后续模型留下维护规范，优先写在正文前的 HTML 注释中。

## 文章生成建议

* 优先保留自然表达，不要把文章统一改成过于生硬的“标准答案”风格。
* 教程类文章可按“背景、步骤、验证、注意事项”组织。
* 笔记类文章可按“主题、要点、例子、易错点”组织。
* 若是系列文章，标题和文件名尽量统一格式，便于后续批量维护。
